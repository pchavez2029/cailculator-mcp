# CAILculator MCP Server Configuration

# Your API key (get from paul@chavezailabs.com)
CAILCULATOR_API_KEY=your_api_key_here

# Auth server endpoint (set automatically in production)
# CAILCULATOR_AUTH_ENDPOINT=https://your-auth-server.railway.app/v1/auth/validate

# Development mode (accepts keys starting with "dev_")
# CAILCULATOR_ENABLE_DEV_MODE=true

# Offline fallback if auth server unreachable
# CAILCULATOR_ENABLE_OFFLINE_FALLBACK=true
